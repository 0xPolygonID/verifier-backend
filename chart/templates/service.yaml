apiVersion: v1
kind: Service
metadata:
  name: {{ include "verifier-backend.fullname" . }}
  labels:
    {{- include "verifier-backend.allLabels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "verifier-backend.selectorLabels" . | nindent 4 }}
